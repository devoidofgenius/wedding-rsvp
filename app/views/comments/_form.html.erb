<%= simple_form_for([@post, @post.comments.build]) do |f| %>
    <p>
        <%= f.input :body, class: "form-control", label: false, input_html: { rows: 3 }  %>
    </p>
    <p>
        <%= f.button :submit, "Submit Comment", class: "btn btn-primary" %>
    </p>
<% end %>